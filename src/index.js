import React from "react"
import ReactDOM from "react-dom/client"
import App from "./App"

alert("zoom in Window for better Experience.")

ReactDOM.createRoot(document.getElementById("root")).render(<App />);

